# Set of custom tools for neudorf
- mainly adjacency matrix manipulation and graph theory functions
- some other utilities I commonly use
- need to install graph-tool yourself `conda install -c conda-forge graph-tool` as there is no pip package available
